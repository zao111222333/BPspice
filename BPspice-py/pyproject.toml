[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "bpspice"
version = "0.1.0"
classifiers = []
requires-python = ">=3.7"

[project.optional-dependencies]
dev = ["pytest"]